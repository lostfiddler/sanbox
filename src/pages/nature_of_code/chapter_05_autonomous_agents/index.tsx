import React from "react";

export default () => {
    return (
        <div>
            <p>
                The concepts here seem to get a bit more complex than the
                earlier chapters. I think it would be best to reference the
                source rather than any notes I might take here.
            </p>
        </div>
    );
};
